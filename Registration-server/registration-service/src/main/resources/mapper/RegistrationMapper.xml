<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.registrationservice.mapper.RegistrationMapper">

    <!-- Find registration by user and activity -->
    <select id="findByUserAndActivity" parameterType="map" resultType="com.cyh.entity.Registration">
        SELECT * FROM t_registration 
        WHERE user_id = #{userId} AND activity_id = #{activityId} AND is_deleted = 0
    </select>

    <!-- Find registrations by status -->
    <select id="findByStatus" parameterType="java.lang.Integer" resultType="com.cyh.entity.Registration">
        SELECT * FROM t_registration WHERE status = #{status} AND is_deleted = 0 ORDER BY create_time DESC
    </select>

    <!-- Find registrations by activity id -->
    <select id="findByActivityId" parameterType="java.lang.Long" resultType="com.cyh.entity.Registration">
        SELECT * FROM t_registration WHERE activity_id = #{activityId} AND is_deleted = 0 ORDER BY create_time DESC
    </select>

</mapper>
